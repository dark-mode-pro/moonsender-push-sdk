// Playground-only service worker stub. On a real site this file lives at your web root and
// contains a single importScripts of the published CDN bundle (see README); here it loads the
// local build so the playground exercises exactly what ships.
importScripts('/dist/sw.js')
